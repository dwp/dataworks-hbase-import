groups:
  - name: master
    jobs:
      - development
      - qa
      - integration
  - name: pull-request
    jobs:
      - pull-request
  - name: update-pipeline
    jobs:
      - update-pipeline
